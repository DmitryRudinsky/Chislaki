# Быстрый старт - Лабораторная работа 3.6

## Численное интегрирование

### Задача

Вычислить определенный интеграл функции **f(x) = [tg(x/2) + ln(x+1)] / [cos²(x-1) + 1]** на отрезке **[-0.5, 1.5]** четырьмя методами:

1. Метод средних прямоугольников
2. Метод трапеций  
3. Метод Симпсона
4. Метод Эйлера

С последующим уточнением по методу Рунге-Ромберга.

---

## 🚀 Быстрый запуск

### 1. Установка зависимостей

```bash
cd 3lab/6
npm install
```

### 2. Запуск приложения

#### Графический интерфейс (рекомендуется)

```bash
npm start
```

Откроется окно Electron с красивым интерфейсом, графиками и таблицами результатов.

#### Консольная версия

```bash
npm run calc
```

Выведет результаты в консоль с полным анализом.

---

## 📊 Что вы увидите

### Графический интерфейс включает:

1. **График функции** - визуализация подынтегральной функции
2. **Сравнение методов** - столбчатые диаграммы для разных шагов
3. **Погрешности** - сравнение точности методов (логарифмическая шкала)
4. **Эффект Рунге-Ромберга** - демонстрация улучшения точности
5. **Таблица результатов** - детальные численные данные

### Консольный вывод показывает:

- Эталонное значение интеграла
- Результаты для шага h = 0.2
- Результаты для шага h/2 = 0.1
- Уточненные значения по Рунге-Ромбергу
- Погрешности всех методов
- Улучшение точности после уточнения

---

## 🎯 Основные результаты

### Эталонное значение
```
I ≈ 0.5514268833
```

### Лучшая точность (без уточнения)
- **Метод Эйлера** с h=0.1: погрешность ~4.7×10⁻⁷

### После уточнения Рунге-Ромберга
- **Метод Симпсона**: погрешность ~1.6×10⁻⁶
- **Улучшение** до 500× для простых методов!

---

## 📈 Порядки точности

| Метод | Порядок | Погрешность (h=0.2) | Погрешность (h=0.1) |
|-------|---------|---------------------|---------------------|
| Средние прямоугольники | O(h²) | 2.3×10⁻³ | 5.8×10⁻⁴ |
| Трапеции | O(h²) | 4.7×10⁻³ | 1.2×10⁻³ |
| Симпсон | O(h⁴) | 6.1×10⁻⁵ | 2.3×10⁻⁶ |
| Эйлер | O(h⁴) | 8.9×10⁻⁶ | 4.7×10⁻⁷ |

---

## 🔬 Метод Рунге-Ромберга

Позволяет уточнить результат без дополнительных вычислений функции!

**Формула:**
```
I* = I_{h/2} + (I_{h/2} - I_h) / (2^p - 1)
```

где p - порядок метода (2 или 4).

**Улучшение точности:**
- Для методов 2-го порядка: **до 500 раз!**
- Для методов 4-го порядка: **до 1.5 раз**

---

## 💡 Выводы

1. **Для быстрых расчетов:** метод трапеций
2. **Для высокой точности:** метод Симпсона или Эйлера
3. **Оптимальное решение:** Симпсон + Рунге-Ромберг
4. **При уменьшении шага в 2 раза:**
   - Методы O(h²): точность ↑ в ~4 раза
   - Методы O(h⁴): точность ↑ в ~16 раз

---

## 📁 Файлы проекта

- `numerical_integration.js` - реализация всех методов
- `graph.js` - визуализация результатов
- `index.html` - пользовательский интерфейс
- `electron-main.js` - Electron приложение
- `README.md` - полная документация

---

## ⚙️ Параметры

Можно изменить в коде `numerical_integration.js`:

```javascript
const a = -0.5;   // начало интервала
const b = 1.5;    // конец интервала
const h1 = 0.2;   // начальный шаг
```

---

## 🎓 Теория

### Метод средних прямоугольников
Значение в середине каждого отрезка × длина отрезка

### Метод трапеций
Линейная интерполяция между соседними точками

### Метод Симпсона
Параболическая интерполяция через 3 точки

### Метод Эйлера
Трапеции + коррекция через производные на концах

---

**Успехов в изучении численных методов! 🚀**

