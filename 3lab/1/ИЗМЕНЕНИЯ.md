# Изменения в лабораторной работе 3.1

## Что было изменено

Вместо выбора только **ближайших точек** к заданной точке x* = 0.413, теперь программа рассматривает **все возможные последовательные интервалы** для интерполяции.

## Ключевые изменения

### 1. lagrange.js

Добавлены новые функции:

- **`getAllConsecutiveIntervals(xValues, yValues, degree)`** - генерирует все возможные последовательные интервалы для заданной степени многочлена
- **`isPointInInterval(xTarget, X)`** - проверяет, находится ли точка x* внутри данного интервала

### 2. graph.js

Полностью переписан для отображения всех интервалов:

- Создаётся отдельный график для каждого интервала
- Интервалы, содержащие x*, выделяются зелёной рамкой и специальным бейджем
- Для каждого графика показываются:
  - Многочлен Лагранжа на всём диапазоне данных
  - Все исходные точки (серые)
  - Узлы интерполяции (зелёные треугольники)
  - Точка x* (звезда)
- Добавлена таблица сравнения всех результатов

### 3. index.html

Обновлены стили:

- Добавлен класс `.interval-contains` для выделения интервалов, содержащих x*
- Добавлен класс `.badge-contains` для визуального маркера
- Добавлены заголовки секций `.section-title`
- Контент теперь генерируется динамически

## Результаты

### Многочлен 2-й степени (P₂)
- **Всего интервалов:** 7
- **Содержат x*:** 2 интервала
  1. Интервал 4: [-0.16, 0.62] → P₂(0.413) = 4.137985
  2. Интервал 5: [0.23, 1.01] → P₂(0.413) = 5.177347

### Многочлен 3-й степени (P₃)
- **Всего интервалов:** 6
- **Содержат x*:** 3 интервала
  1. Интервал 3: [-0.55, 0.62] → P₃(0.413) = 3.532009
  2. Интервал 4: [-0.16, 1.01] → P₃(0.413) = 4.647006
  3. Интервал 5: [0.23, 1.40] → P₃(0.413) = 5.973281

## Визуализация

На графиках можно наблюдать:

1. **Различное поведение** интерполяционных многочленов в зависимости от выбранных узлов
2. **Эффект Рунге** - осцилляции при экстраполяции за пределы интервала
3. **Точность интерполяции** - интервалы, содержащие x*, дают более надёжные результаты
4. **Влияние выбора узлов** - разные наборы узлов дают разные значения в точке x*

## Запуск

```bash
cd /Users/gula-dmitrii/Chislacki/3lab/1
npm start
```

Или для просмотра в консоли:

```bash
node lagrange.js
```

